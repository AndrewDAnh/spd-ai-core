version: "3.9"

services:
  spd-api:
    build: .
    image: spd-mvp:latest
    env_file:
      - .env
    volumes:
      - model-data:/app/data
    ports:
      - "9090:9000"
    networks:
      - spd-network

volumes:
  model-data:

networks:
  spd-network:
    name: spd-network
